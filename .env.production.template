# ============================================
# LEMAN NEGÓCIOS IMOBILIÁRIOS - PRODUCTION ENV
# ============================================
# Template para variáveis de ambiente de produção
# Google Cloud Run + Supabase
#
# INSTRUÇÕES:
# 1. Copie este arquivo para .env.production
# 2. Preencha os valores com suas credenciais reais
# 3. Configure no Google Cloud Run via Console ou gcloud CLI
# 4. NUNCA commite este arquivo com valores reais
# ============================================

# ============================================
# 🔴 OBRIGATÓRIAS (Sistema não inicia sem elas)
# ============================================

# Database: Supabase PostgreSQL Connection String
# Formato: postgresql://postgres.[project-ref]:[password]@aws-0-us-east-1.pooler.supabase.com:6543/postgres
# Onde obter: Supabase Dashboard > Settings > Database > Connection String (Transaction Pooler)
DATABASE_URL=YOUR_SUPABASE_CONNECTION_STRING_HERE

# JWT Secret: Chave mestra para assinatura de tokens de autenticação
# Gerar com: openssl rand -base64 32
# Mínimo: 32 caracteres
JWT_SECRET=YOUR_JWT_SECRET_HERE

# ============================================
# 🟡 RECOMENDADAS (Storage de arquivos)
# ============================================

# Storage: AWS S3 / Google Cloud Storage / MinIO
# Necessário para upload de imagens de imóveis e documentos
STORAGE_BUCKET=YOUR_BUCKET_NAME_HERE
STORAGE_REGION=us-east-1
STORAGE_ACCESS_KEY=YOUR_ACCESS_KEY_HERE
STORAGE_SECRET_KEY=YOUR_SECRET_KEY_HERE

# Storage Endpoint (opcional - apenas para S3-compatible como MinIO)
# Deixe vazio para AWS S3 ou GCS
STORAGE_ENDPOINT=

# ============================================
# 🟢 OPCIONAIS (Integrações externas)
# ============================================

# N8N: Webhooks para automações de WhatsApp e IA
# Onde obter: N8N Dashboard > Workflow > Webhook URL
N8N_LEAD_WEBHOOK_URL=
N8N_WHATSAPP_WEBHOOK_URL=
N8N_API_KEY=

# OAuth: Autenticação via servidor externo (Manus OAuth)
# Deixe vazio se usar apenas autenticação local
OAUTH_SERVER_URL=
OWNER_OPEN_ID=

# Forge API: Integrações com APIs externas
# Deixe vazio se não usar
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=

# ============================================
# 🔧 SISTEMA (Configuradas automaticamente)
# ============================================

# Ambiente de execução
NODE_ENV=production

# Porta do servidor (Cloud Run define automaticamente)
# Não altere este valor
PORT=8080

# App ID (identificador da aplicação)
VITE_APP_ID=leman-negocios-imobiliarios

# ============================================
# 📝 NOTAS IMPORTANTES
# ============================================
#
# 1. DATABASE_URL e JWT_SECRET são OBRIGATÓRIAS
#    - O sistema não iniciará sem elas
#
# 2. STORAGE_* são RECOMENDADAS
#    - Sem elas, upload de imagens não funcionará
#    - Use Google Cloud Storage se estiver no GCP
#
# 3. N8N_* são OPCIONAIS
#    - Necessárias apenas se usar automações de WhatsApp
#    - Sistema funciona normalmente sem elas
#
# 4. OAUTH_* são OPCIONAIS
#    - Necessárias apenas se usar OAuth externo
#    - Sistema tem autenticação local por padrão
#
# 5. SEGURANÇA
#    - NUNCA exponha este arquivo publicamente
#    - Use Google Secret Manager para produção
#    - Rotacione JWT_SECRET periodicamente
#
# ============================================
